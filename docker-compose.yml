services:
  mongo:
    image: mongo:7
    container_name: dc-project-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: dc-project
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
